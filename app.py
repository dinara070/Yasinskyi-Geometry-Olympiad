import streamlit as st
import requests
from bs4 import BeautifulSoup
from urllib.parse import urljoin, unquote
import io
import zipfile
import pandas as pd

# --- 1. Налаштування сторінки ---
st.set_page_config(
    page_title="Геометрична олімпіада ім. В. Ясінського",
    page_icon="📐",
    layout="wide",
    initial_sidebar_state="expanded"
)

# --- 2. CSS Стилізація (щоб було схоже на сайт) ---
st.markdown("""
    <style>
    /* Основні шрифти та кольори */
    .main {
        background-color: #ffffff;
    }
    h1, h2, h3 {
        font-family: 'Helvetica', sans-serif;
        color: #2c3e50;
    }
    /* Стиль для карток */
    .info-card {
        padding: 20px;
        border-radius: 10px;
        background-color: #f8f9fa;
        border: 1px solid #e9ecef;
        margin-bottom: 20px;
    }
    /* Стиль для кнопки скачування */
    .stButton>button {
        width: 100%;
        border-radius: 5px;
        font-weight: bold;
    }
    </style>
    """, unsafe_allow_html=True)

# --- 3. Функції ---

def fetch_pdf_links(target_url):
    """Парсинг PDF посилань з сайту"""
    try:
        headers = {"User-Agent": "Mozilla/5.0"}
        response = requests.get(target_url, headers=headers, timeout=10)
        response.raise_for_status()
        soup = BeautifulSoup(response.text, 'html.parser')
        links = soup.find_all('a', href=True)
        pdf_links = []
        for link in links:
            href = link['href']
            if href.lower().endswith('.pdf'):
                full_url = urljoin(target_url, href)
                pdf_links.append(full_url)
        return pdf_links
    except Exception as e:
        st.error(f"Помилка з'єднання: {e}")
        return []

# --- 4. Навігація (Sidebar) ---
with st.sidebar:
    st.image("https://yasinskyi-geometry-olympiad.com/images/main-logo.png", width=100)
    st.title("Меню")
    
    page = st.radio(
        "Перейти до:",
        ["Про олімпіаду", "Олімпіада 2025", "Задачі (Архів)", "Історія", "Контакти"],
        label_visibility="collapsed"
    )
    
    st.markdown("---")
    st.caption("© 2025 Yasinskyi Geometry Olympiad Copy")
    st.caption("Developed with Python & Streamlit")

# --- 5. Логіка сторінок ---

# === СТОРІНКА: ПРО ОЛІМПІАДУ ===
if page == "Про олімпіаду":
    # Заголовок з картинкою (якщо є на сайті)
    col1, col2 = st.columns([1, 2])
    with col1:
         # Спробуємо взяти фото Ясінського з сайту
        st.image("https://yasinskyi-geometry-olympiad.com/images/yasinskyi.jpg", caption="В'ячеслав Ясінський", use_container_width=True)
    with col2:
        st.title("Геометрична олімпіада імені В'ячеслава Ясінського")
        st.markdown("""
        **Геометрична олімпіада імені В'ячеслава Ясінського** — це щорічне змагання, яке об'єднує поціновувачів геометричних задач. 
        Вперше вона була проведена у **2017 році**.
        
        Олімпіада названа на честь **В'ячеслава Андрійовича Ясінського** — відомого українського вчителя математики, доцента, заслуженого вчителя України, майстра створення красивих олімпіадних задач.
        """)

    st.markdown("---")
    
    st.subheader("Деталі олімпіади")
    st.info("""
    Олімпіада надає чудову можливість перевірити свої навички розв'язування олімпіадних геометричних задач. 
    Складність запропонованих задач відповідає рівню національних олімпіад.
    """)
    
    c1, c2, c3 = st.columns(3)
    with c1:
        st.markdown("### 🎯 **Для кого**")
        st.write("Учні 8, 9 та 10-11 класів")
    with c2:
        st.markdown("### 💻 **Формат**")
        st.write("Онлайн (дистанційно)")
    with c3:
        st.markdown("### ⏳ **Тривалість**")
        st.write("4 години")

# === СТОРІНКА: ОЛІМПІАДА 2025 ===
elif page == "Олімпіада 2025":
    st.title("IX Геометрична олімпіада (2025/2026)")
    
    st.warning("⚠️ **Зверніть увагу:** Наступна олімпіада відбудеться у **листопаді 2026 року**.")
    
    st.markdown("### Правила участі")
    st.markdown("""
    <div class="info-card">
    <ul>
        <li>Пропонується для розв'язання <b>5 геометричних задач</b>.</li>
        <li>Кожна задача оцінюється від <b>0 до 7 балів</b>.</li>
        <li>Завдання розраховані на учнів останніх чотирьох класів загальноосвітньої школи.</li>
    </ul>
    </div>
    """, unsafe_allow_html=True)

    st.subheader("Реєстрація")
    st.write("Попередня реєстрація **не потрібна**.")
    st.write("У день олімпіади на сайті з'явиться форма, через яку ви зможете надіслати розв'язки (PDF) українською або англійською мовою.")
    
    with st.expander("Що буде в реєстраційній формі?"):
        st.markdown("""
        * Повне ім'я та прізвище
        * Адреса електронної пошти
        * Країна та місто
        * Школа та клас
        * Файл із розв'язками
        """)

# === СТОРІНКА: ЗАДАЧІ (АРХІВ) ===
elif page == "Задачі (Архів)":
    st.title("📚 Архів задач")
    st.write("Запрошуємо переглянути задачі та розв'язки минулих олімпіад.")
    
    target_url = "https://yasinskyi-geometry-olympiad.com/"
    
    # Імітація списку років як на сайті
    st.markdown("### Доступні матеріали")
    
    years = range(2025, 2016, -1)
    col1, col2 = st.columns(2)
    
    with col1:
        st.markdown("**Рік**")
        for year in years:
            st.write(f"🔹 {year} рік")
            
    with col2:
        st.markdown("**Статус**")
        for year in years:
            st.caption(f"Доступно на сайті (Умови + Розв'язки)")

    st.markdown("---")
    st.subheader("📥 Автоматичне завантаження")
    st.write("Ви можете завантажити всі матеріали одним архівом, натиснувши кнопку нижче.")

    if st.button("🚀 Знайти та завантажити всі PDF (ZIP)"):
        status_container = st.container()
        progress_bar = st.progress(0)
        
        with status_container:
            st.info("Сканування сайту...")
            pdf_links = fetch_pdf_links(target_url)
            
            if pdf_links:
                st.success(f"Знайдено {len(pdf_links)} файлів. Завантаження...")
                
                zip_buffer = io.BytesIO()
                with zipfile.ZipFile(zip_buffer, "w") as zf:
                    for i, file_url in enumerate(pdf_links):
                        file_name = unquote(file_url.split('/')[-1])
                        try:
                            r = requests.get(file_url, headers={"User-Agent": "Mozilla/5.0"})
                            zf.writestr(file_name, r.content)
                        except:
                            pass
                        progress_bar.progress((i + 1) / len(pdf_links))
                
                zip_buffer.seek(0)
                st.download_button(
                    label="💾 Зберегти yasinskyi_archive.zip",
                    data=zip_buffer,
                    file_name="yasinskyi_geometry_olympiad.zip",
                    mime="application/zip",
                    type="primary"
                )
            else:
                st.error("Не вдалося знайти файли. Можливо, структура сайту змінилася.")

# === СТОРІНКА: ІСТОРІЯ ===
elif page == "Історія":
    st.title("📊 Статистика та результати")
    
    # Дані з сайту
    data = {
        'Рік': ['2017', '2018', '2019', '2020', '2021', '2022', '2023', '2024', '2025'],
        'Учасники': [58, 76, 129, 136, 169, 145, 100, 58, 139],
        'Країни': [1, 1, 1, 1, 1, 2, 3, 6, 7]
    }
    df = pd.DataFrame(data)
    
    st.subheader("Динаміка кількості учасників")
    st.bar_chart(df.set_index('Рік')['Учасники'], color="#3498db")
    
    st.subheader("Географія")
    st.line_chart(df.set_index('Рік')['Країни'], color="#e74c3c")
    
    st.markdown("""
    **Цікаві факти:**
    * 2021 рік став рекордним за кількістю учасників (169).
    * З 2022 року олімпіада почала активно залучати міжнародних учасників (до 7 країн у 2025 році).
    """)

# === СТОРІНКА: КОНТАКТИ ===
elif page == "Контакти":
    st.title("📬 Контакти")
    
    col1, col2 = st.columns(2)
    
    with col1:
        st.markdown("""
        Геометрична олімпіада імені В'ячеслава Ясінського запрошує математиків, педагогів та авторів до співпраці.
        
        Ми завжди шукаємо оригінальні та цікаві геометричні задачі.
        """)
        st.info("📧 **Email:** yasinskyi.geometry.olympiad@gmail.com")
        
    with col2:
        # Форма зворотнього зв'язку (імітація)
        st.write("Напишіть нам:")
        name = st.text_input("Ваше ім'я")
        msg = st.text_area("Повідомлення")
        if st.button("Надіслати"):
            st.success("Дякуємо! (Це демо-форма, повідомлення не було відправлено)")
