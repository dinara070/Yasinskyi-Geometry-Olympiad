import streamlit as st
import requests
from bs4 import BeautifulSoup
from urllib.parse import urljoin, unquote
import io
import zipfile
import pandas as pd
import os
from datetime import datetime

# --- 1. Налаштування сторінки ---
st.set_page_config(
    page_title="Портал Кафедри алгебри і методики навчання математики",
    page_icon="🎓",
    layout="wide",
    initial_sidebar_state="expanded"
)

# --- 2. CSS та Брендування ВДПУ ---
st.markdown("""
    <style>
    /* Стилі для заголовків та кольорів університету */
    .header-university {
        color: #800000; /* Бордовий колір */
        font-family: 'Times New Roman', serif;
        text-align: center;
        margin-bottom: 0px;
    }
    .header-faculty {
        color: #2c3e50;
        font-family: sans-serif;
        text-align: center;
        font-size: 1.2rem;
        margin-top: 0px;
        font-weight: bold;
    }
    .header-dept {
        color: #555;
        text-align: center;
        font-style: italic;
        margin-bottom: 20px;
        border-bottom: 2px solid #800000;
        padding-bottom: 10px;
    }
    .info-card {
        padding: 20px;
        border-radius: 10px;
        background-color: #f8f9fa;
        border-left: 5px solid #800000;
        box-shadow: 2px 2px 5px rgba(0,0,0,0.1);
        margin-bottom: 20px;
    }
    .stButton>button {
        background-color: #800000;
        color: white; 
        border-radius: 5px;
    }
    .stButton>button:hover {
        background-color: #a00000;
        color: white;
    }
    </style>
    """, unsafe_allow_html=True)

# --- 3. Змінні ---
# Переконайтеся, що файл yasinskyi.png та logo.png є у папці
PHOTO_YASINSKYI = "yasinskyi.png"
LOGO_FILE = "logo.png" 

# --- 4. Функції ---
def fetch_pdf_links(target_url):
    """Парсинг PDF посилань"""
    try:
        headers = {"User-Agent": "Mozilla/5.0"}
        response = requests.get(target_url, headers=headers, timeout=10)
        response.raise_for_status()
        soup = BeautifulSoup(response.text, 'html.parser')
        links = soup.find_all('a', href=True)
        pdf_links = []
        for link in links:
            href = link['href']
            if href.lower().endswith('.pdf'):
                full_url = urljoin(target_url, href)
                pdf_links.append(full_url)
        return pdf_links
    except Exception as e:
        st.error(f"Помилка з'єднання: {e}")
        return []

# --- 5. Шапка (Header) ---
col_l, col_c, col_r = st.columns([1, 6, 1])
with col_l:
    if os.path.exists(LOGO_FILE):
        st.image(LOGO_FILE, width=100)
    else:
        st.write("🏛️") 

with col_c:
    st.markdown('<h2 class="header-university">Вінницький державний педагогічний університет<br>імені Михайла Коцюбинського</h2>', unsafe_allow_html=True)
    st.markdown('<div class="header-faculty">Факультет математики, фізики і комп\'ютерних наук</div>', unsafe_allow_html=True)
    st.markdown('<div class="header-dept">Кафедра алгебри і методики навчання математики</div>', unsafe_allow_html=True)

# --- 6. Навігація ---
with st.sidebar:
    st.title("Навігація")
    menu = st.radio(
        "Оберіть розділ:",
        ["🏠 Головна кафедри", "📐 Олімпіада ім. В. Ясінського", "📝 Методичний кабінет", "📞 Контакти"],
        label_visibility="collapsed"
    )
    st.markdown("---")
    st.info("Розроблено для підтримки навчального процесу та олімпіадного руху.")

# --- 7. Логіка розділів ---

# === ГОЛОВНА СТОРІНКА КАФЕДРИ ===
if menu == "🏠 Головна кафедри":
    st.subheader("Вітаємо на цифровому порталі кафедри!")
    
    st.markdown("""
    <div class="info-card">
    Цей ресурс створено для студентів, викладачів та вчителів математики. Тут ви знайдете:
    <ul>
        <li>Матеріали для підготовки до олімпіад.</li>
        <li>Інструменти для створення методичних карток.</li>
        <li>Архіви задач та наукових робіт.</li>
    </ul>
    </div>
    """, unsafe_allow_html=True)

    col1, col2 = st.columns(2)
    with col1:
        st.markdown("### 📢 Оголошення")
        st.info(f"📅 **{datetime.now().strftime('%d.%m.%Y')}**: Оновлено базу олімпіадних задач.")
        st.write("🔹 Запрошуємо до участі у конкурсі творчих робіт.")
        
    with col2:
        st.markdown("### 🔬 Наукова робота")
        st.progress(85, text="Методика навчання алгебри")
        st.progress(70, text="Олімпіадна геометрія")

# === ОЛІМПІАДА ЯСІНСЬКОГО ===
elif menu == "📐 Олімпіада ім. В. Ясінського":
    st.markdown("### 🏆 Олімпіада геометричної творчості імені В. А. Ясінського")
    
    tab1, tab2, tab3 = st.tabs(["ℹ️ Про олімпіаду", "📚 Архів задач", "📊 Статистика"])
    
    with tab1:
        col_img, col_text = st.columns([1, 2])
        with col_img:
            if os.path.exists(PHOTO_YASINSKYI):
                st.image(PHOTO_YASINSKYI, use_container_width=True)
                st.caption("**В’ячеслав Андрійович Ясінський** (1957-2015)")
            else:
                st.warning("Фото відсутнє")
        
        with col_text:
            st.markdown("""
            У **2017 році** кафедра алгебри і методики навчання математики Вінницького державного педагогічного університету імені Михайла Коцюбинського започаткувала проведення **Олімпіади геометричної творчості імені В. А. Ясінського**.
            """)
            
            st.markdown("#### Напрямки проведення заходу:")
            st.markdown("""
            Захід проходить у ВДПУ ім. М. Коцюбинського за такими напрямками:
            * 🧑‍🏫 **Турнір методичних знахідок** (для вчителів геометрії).
            * ✏️ **Змагання з розв’язування геометричних задач** (для учнів).
            * 🎨 **Конкурс творчих робіт з геометрії** (для колективів учнів під керівництвом учителя).
            """)
            
            st.info("📜 Пропонуємо ознайомитись із **Положенням про Олімпіаду геометричної творчості імені В. А. Ясінського** (зверніться на кафедру або завантажте нижче).")

    with tab2:
        st.write("Автоматизований модуль для завантаження методичних матеріалів олімпіади.")
        if st.button("🚀 Завантажити повний архів задач (PDF)"):
            with st.spinner("З'єднання з сервером олімпіади..."):
                pdf_links = fetch_pdf_links("https://yasinskyi-geometry-olympiad.com/")
                if pdf_links:
                    zip_buffer = io.BytesIO()
                    with zipfile.ZipFile(zip_buffer, "w") as zf:
                        for i, url in enumerate(pdf_links):
                            try:
                                r = requests.get(url, headers={"User-Agent": "Mozilla/5.0"})
                                fname = unquote(url.split('/')[-1])
                                zf.writestr(fname, r.content)
                            except: pass
                    zip_buffer.seek(0)
                    st.success("Архівація завершена!")
                    st.download_button("💾 Зберегти ZIP", zip_buffer, "yasinskyi_full_archive.zip", "application/zip")
                else:
                    st.error("Помилка доступу до джерела.")

    with tab3:
        data = {'Рік': ['2021', '2022', '2023', '2024', '2025'], 'Учасники': [169, 145, 100, 58, 139]}
        st.bar_chart(pd.DataFrame(data).set_index('Рік'))

# === МЕТОДИЧНИЙ КАБІНЕТ ===
elif menu == "📝 Методичний кабінет":
    st.markdown("### 🛠️ Генератор методичної картки уроку")
    with st.form("lesson_plan"):
        col1, col2 = st.columns(2)
        with col1:
            topic = st.text_input("Тема уроку", "Теорема Піфагора")
            grade = st.selectbox("Клас", ["7 клас", "8 клас", "9 клас", "10 клас", "11 клас"])
        with col2:
            goal = st.text_area("Мета уроку", "Сформувати вміння застосовувати теорему...")
            type_lesson = st.selectbox("Тип уроку", ["Засвоєння нових знань", "Практикум", "Узагальнення"])
        
        submitted = st.form_submit_button("🖨️ Згенерувати картку")
    
    if submitted:
        st.success("Картка згенерована!")
        st.code(f"""
        ПЛАН-КОНСПЕКТ УРОКУ
        Клас: {grade} | Тема: {topic}
        Тип: {type_lesson}
        Мета: {goal}
        """, language="text")

# === КОНТАКТИ ===
elif menu == "📞 Контакти":
    st.markdown("### 📍 Наша адреса")
    
    col1, col2 = st.columns(2)
    
    with col1:
        st.markdown("""
        **21100, м. Вінниця, вул. Острозького, 32** Факультет математики, фізики і комп'ютерних наук  
        **Корпус 3, поверх 5.**
        """)
        
    with col2:
        st.markdown("### ☎️ Контактні телефони:")
        st.write("🧑‍🏫 **Коношевський Олег Леонідович**")
        st.caption("доцент кафедри алгебри і методики навчання математики")
        st.write("📞 `(067) 29-010-78`")
        
        st.markdown("---")
        
        st.write("🧑‍🏫 **Панасенко Олексій Борисович**")
        st.caption("старший викладач кафедри алгебри і методики навчання математики")
        st.write("📞 `(067) 215-15-71`")
        st.write("📞 `(063) 153-04-67`")
    
    st.markdown("---")
    st.map(pd.DataFrame({'lat': [49.2325], 'lon': [28.4833]})) # Координати ВДПУ

# --- Підвал ---
st.markdown("---")
st.markdown("<div style='text-align: center; color: gray;'>© 2025 ВДПУ ім. М. Коцюбинського | Кафедра алгебри і методики навчання математики</div>", unsafe_allow_html=True)
